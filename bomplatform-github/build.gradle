bootJar { enabled = false }
jar { enabled = true }

dependencies {
    implementation project(path:':bomplatform-event', configuration: 'default')
    implementation group: 'com.google.code.gson', name: 'gson', version: '2.8.6'
    // github
}
